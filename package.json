{
  "name": "node_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev_server": "cd server && nodemon server",
    "server": "cd server && node server",
    "client": "cd client && npm start",
    "dev": "concurrently --kill-others-on-fail \" npm run dev_server\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
